export const filterFieldOfGithubProfile = ({
  login,
  following,
  followers,
  public_gists,
  public_repos,
  twitter_username,
  bio,
  hireable,
  email,
  location,
  blog,
  company,
  name,
  site_admin,
  type,
  html_url,
  url,
  avatar_url,
  node_id,
  id,
}: any) => ({
  login,
  following,
  followers,
  public_gists,
  public_repos,
  twitter_username,
  bio,
  hireable,
  email,
  location,
  blog,
  company,
  name,
  site_admin,
  type,
  html_url,
  url,
  avatar_url,
  node_id,
  id,
});

export const filterFieldOfGithubRepo = ({
  default_branch,
  open_issues,
  topics,
  license,
  size,
  description,
  url,
  full_name,
  node_id,
  id,
}: any) => ({
  default_branch,
  open_issues,
  topics,
  license,
  size,
  description,
  url,
  full_name,
  node_id,
  id,
});
